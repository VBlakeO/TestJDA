using UnityEngine;

public class AmazoniaItem : MonoBehaviour
{
    public int itemId = 0;
    
    private void Start()
    {
        //Amazonia.m_Instance.instantiatedItems.Add(gameObject);
    }

    public void RemoveItem()
    {
        //Amazonia.m_Instance.instantiatedItems.Remove(gameObject);
    }
}
